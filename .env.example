# 複製成 .env 後填入。.env 已被 .gitignore 擋掉，不會進版控。

# 必填：Anthropic API 金鑰，到 console.anthropic.com 申請
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxx

# 若你的金鑰是「綁定身分」的（personal 或 service account key），這欄必填。
# 到 console.anthropic.com → Settings → Workspaces，複製 ID 欄位（wrkspc_ 開頭）。
# 建立時就綁定單一 workspace 的金鑰可以留空。
ANTHROPIC_WORKSPACE_ID=

# 搜尋查證用（選填）。沒填就跳過查證，行程照常出得來。
# 到 tavily.com 申請，免費額度每月 1000 次。
TAVILY_API_KEY=

# 地點自動完成與定位反查（選填）。沒填就退回純文字輸入，其他功能不受影響。
# Google Cloud Console 啟用「Places API (New)」與「Geocoding API」後建立金鑰。
# 金鑰只在後端使用，不會出現在網頁原始碼裡。
GOOGLE_MAPS_API_KEY=

# 行程編排用的模型（會推理、算動線，別省這一步）
PLANNER_MODEL=claude-sonnet-4-5

# 之後要做資料萃取時用的便宜模型（目前尚未使用）
EXTRACT_MODEL=claude-haiku-4-5

# 伺服器埠號
PORT=8787

# 每個 IP 每分鐘最多幾次排行程
RATE_LIMIT_PER_MIN=6

# 自動完成每打一個字問一次，額度另計
PLACES_RATE_LIMIT_PER_MIN=60

# 相同條件的結果快取幾秒（0 = 不快取）
CACHE_TTL_SECONDS=600

# ─────────────────────────────────────────────────────────────
# TDX：只用來查「高鐵對號座還有沒有位」（O 有位 / L 剩少量 / X 售完）
#
# 班次與票價走 PTX，不需要憑證；只有剩餘座位 PTX 沒有，非 TDX 不可。
# 台鐵沒有這種資料，所以這兩把金鑰只影響高鐵。
# 不填就不顯示座位狀態，其他一切照常。
#
# 申請：https://tdx.transportdata.tw 註冊會員 → 會員專區 → API 金鑰
# ─────────────────────────────────────────────────────────────
TDX_CLIENT_ID=
TDX_CLIENT_SECRET=
